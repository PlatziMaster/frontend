import React from 'react';

const Categories = (props) => {
    return (
        <div  className="container-pro">
            {props.children}
        </div>
     
    );
};
  
export default Categories;