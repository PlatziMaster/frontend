import React from 'react';
import  getData  from '../utils/getData';

const Header = ({children}) => {
            return(
        <>
            {children}
        </>
    )
};

export default Header;
