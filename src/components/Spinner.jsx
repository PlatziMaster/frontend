import React from 'react';

const Spinner = () => {
  return (
    <div className="spinner">
      <div class="lds-dual-ring">
        <p>Loading</p>
      </div>
    </div>
  );
};

export default Spinner;
