@import '../variables.styl'

.Academic-list
  gap 20px

.Academic-item
  & > p:last-child
    color $gray
    justify-self flex-end
  & .badge
    margin-left 10px
    color $primary-color
    background-color rgba($primary-color, 0.2)