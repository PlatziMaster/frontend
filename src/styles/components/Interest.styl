.Interest {
  &__list {
    display flex 
    flex-wrap wrap
    padding 0
  }
  &-item {
    background white
    color var(--color-title)
    display inline-flex
    font-weight 700
    padding 5px
    text-transform uppercase
    margin-right 0.5rem
    margin-bottom 0.5rem
    font-size 0.8rem
  }
}