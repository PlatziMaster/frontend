.Main-interest
  width: 47.5%
  margin-inline-end: 5px
  .Interest-title
    h2-title()
  .Interest-container
    display: flex
    justify-content: space-between
    align-items: center
    flex-wrap: wrap
    width: 100%
    height: auto
    .Interest-item
      display: flex
      justify-content: space-between
      align-items: start
      flex-wrap: wrap
      width: 48%
      margin-block-start: 15px
      box-styles()
      h4
        width: 100%
        text-align: center
        h4-title()